include ../mixins/forms.jade
include ../mixins/avatar-box.jade

.header-box Account
	if user.id && adminArea
		button.btn.btn-lg.btn-danger.pull-right(data-action='delete-user') Delete
form#user-form.content-box
	#user-general-message-group.alert(role='alert'): span.message
	unless user.id
		+input('username', 'Username*')
	else
		+static('username', 'Username', user.username)
	+input('name', 'Name*')
	+input('email', 'Email*')
	if adminArea
		+checkbox('admin', 'Is admin user')(disabled=isOwnUser)
		+checkbox('canCreateGroups', 'Is allowed to create groups')(disabled=isOwnUser)
		+checkbox('canCreateRepositories', 'Is allowed to create private repositories')(disabled=isOwnUser)
	button.btn.btn-lg.btn-success.pull-right(data-action='save-user')
		if user.id
			| Save
		else
			| Create
	.clear
if user.id
	+avatar-box('user', user.username, true)
	.header-box Password
	form#password-form.content-box
		#password-general-message-group.alert(role='alert'): span.message
		+input('password', 'Password', 'password')
		+input('password2', 'Password (repeated)', 'password')
		button.btn.btn-lg.btn-success.pull-right(data-action='save-password') Save
		.clear