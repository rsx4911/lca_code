include comment-entry-mixins.jade

.header-box
.content-box
	if !comments.length
		.no-content-message No comments yet
	each comment, index in comments
		+comment-entry(comment, null, true)
		if index < comments.length-1
			hr