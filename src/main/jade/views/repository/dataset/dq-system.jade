include ./mixins.jade

+menubar
.content-box
	.model-left-content
		+header
		.content
			h4(data-path='indicators')=getLabel('indicators')
			+data-quality-matrix(dataset, false)
			if dataset.hasUncertainties
				hr
				h4=getLabel('uncertainties')
				+data-quality-matrix(dataset, true)
	+meta
		hr
		strong=getLabel('source')
		div: +ref('source', null, '-')
	.clear