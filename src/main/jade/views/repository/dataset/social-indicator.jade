include ./mixins.jade

+menubar
.content-box
	.model-left-content
		+header
		.content
			table.table.no-head
				tbody
					+field-row('unitOfMeasurement')
					+field-row('evaluationScheme')
					+field-row('activityVariable')
					+ref-row('activityQuantity')
					if dataset.activityUnit || getValue(compareTo, activityUnit) || reviewMode
						+field-row('activityUnit.name')
	+meta
	.clear