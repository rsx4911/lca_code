include ./mixins.jade

+menubar
.content-box
	.model-left-content
		+header
		.content
			table.table.no-head
				tbody
					+field-row('code', true, true)
					+field-row('conversionFactor', true, true)
					+ref-row('referenceCurrency')
	+meta
	.clear