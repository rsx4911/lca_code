include ./mixins.jade

+menubar
.content-box
	.model-left-content
		+header
		.content
			table.table.no-head
				tbody
					+field-row('code', true, true)
					+field-row('longitude', true, true)
					+field-row('latitude', true, true)
			#map.map
	+meta
	.clear