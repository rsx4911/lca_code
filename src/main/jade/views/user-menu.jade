include mixins/forms.jade

mixin menu-items()
	a.search-link-icon(href='search')
		span.glyphicon.glyphicon-search  
	if isAdmin
		a(href='administration')
			span.glyphicon.glyphicon-wrench(data-toggle='tooltip' data-placement='bottom' title='Admin area')
	if settings['TASKS_ENABLED'] === 'true'
		a.no-deco.tasks(href='tasks' data-toggle='tooltip' data-placement='bottom' title='Tasks')
			span.task-icon.glyphicon.glyphicon-inbox
			span.task-count(data-count=activeTasks)=activeTasks	
	if websocketSupported && settings['MESSAGING_ENABLED'] === 'true'
		a.no-deco.messages(href='messages' data-toggle='tooltip' data-placement='bottom' title='Inbox')
			span.message-icon.glyphicon.glyphicon-comment(class=unreadMessages?'new-messages':null)
			span.message-count(class=unreadMessages?null:'hidden' data-count=unreadMessages)=unreadMessages
	a.logout(href='#')
		span.glyphicon.glyphicon-log-out(data-toggle='tooltip' data-placement='bottom' title='Logout')
	if settings['COMMENTS_ENABLED'] === 'true'
		a.toggle-review(href='#')
			span.glyphicon.glyphicon-pencil(data-toggle='tooltip' data-placement='bottom' title='Review mode #{reviewMode?"on":"off"}' data-active=reviewMode)
	if isAdmin
		a.toggle-debug(href='#')
			span.glyphicon(class=debugMode?'glyphicon-eye-open':'glyphicon-eye-close' data-toggle='tooltip' data-placement='bottom' title='Debugging #{debugMode?"on":"off"}')

.pull-left.has-feedback
	+input('global-search')(placeholder='Search')
		span.glyphicon.glyphicon-search.form-control-feedback
if isPublic
	a.login(href='login')
		span.glyphicon.glyphicon-log-in(data-toggle='tooltip' data-placement='bottom' title='Login')
else
	.pull-right.responsive-menu.dropdown
		a(href='#' data-toggle='dropdown'): span.glyphicon.glyphicon-cog
		.dropdown-menu
			+menu-items()
		.default-menu
			+menu-items()
