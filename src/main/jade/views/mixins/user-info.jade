mixin user-info(id, name, type)
	- var lcType = type ? type.toLowerCase() : 'user';		
	span.user-info&attributes(attributes)
		img.avatar.avatar-small(src='ws/#{lcType}/avatar/#{id}')
		span.username #{name} 
			if type
				| (#{type})
			block
