include ../mixins/paging.jade

.list-container
	each repo in data
		- var repoFull = repo.group + '/' + repo.name;
		.list-entry
			img.avatar(src='/ws/repository/avatar/#{repoFull}')
			a.follow(href='/repository/#{repoFull}')
				strong #{repoFull}
	+paging(page, pageCount)