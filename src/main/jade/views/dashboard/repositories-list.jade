include ../mixins/paging.jade

.list-container
	each repo in data
		.list-entry
			img.avatar(src='ws/repository/avatar/#{repo.group}/#{repo.name}')
			a.follow(href='#{repo.group}/#{repo.name}')
				strong #{repo.group}/#{repo.name}
	+paging(resultInfo)