include ./mixins.pug

+menubar
.content-box
	.model-left-content
		+header
		.content
			table.table.no-head
				tbody
					- var getParameterType = function(value, parent) { return parent ? (parent.isIputParameter ? 'Input' : 'Dependant') : null;};
					+field-row('isIputParameter', getParameterType)
					- var getFormula = function (value, parent) { return parent ? (parent.isIputParameter ? parent.value : (parent.formula + ' = ' + parent.value)) : null;}; 
					+field-row(dataset.isIputParameter?'value':'formula', getFormula)
	+meta
	.clear