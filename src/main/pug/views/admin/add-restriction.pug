include ../mixins/forms.pug

#add-restriction
	#add-restriction-general-message-group.alert(role='alert'): span.message
	if isNew
		+input('name', 'Restriction name')
	else
		+input('name', 'Restriction name', 'hidden')
	+textarea('ref-ids', 'Data set reference ids (one id per line)')(rows=10)