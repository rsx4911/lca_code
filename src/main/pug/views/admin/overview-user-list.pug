include ../mixins/paging.pug

each user in data
	div
		a.follow(href='administration/user/profile/' + user.username)=user.name
		|  
		span.badge.pull-right(class=user.deactivated?'badge-danger':'badge-success')=user.deactivated?'Inactive':'Active'
		.clear
+paging(resultInfo, null, pageSizeId)
