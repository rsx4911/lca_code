include ../mixins/paging.pug

each repo in data
	div
		a.follow(href=repo.group + '/' + repo.name) #{repo.group}/#{repo.name}
		button.btn.btn-xs.btn-default(data-action='reindex-repository' data-group=repo.group data-repository=repo.name) Reindex
+paging(resultInfo, null, pageSizeId)
