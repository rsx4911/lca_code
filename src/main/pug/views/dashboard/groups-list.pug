include ../mixins/paging.pug

.list-container
	each group in data
		.list-entry
			img.avatar(src='ws/group/avatar/' + group.name, aria-label='Avator of group ' + group.name)
			a.follow(href='groups/' + group.name)
				strong=group.name
	+paging(resultInfo)